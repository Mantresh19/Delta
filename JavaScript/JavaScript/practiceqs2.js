// Qs1
const random = Math.floor(Math.random() * 6) + 1;
console.log(random);

// Qs2
let car = {
    name: 'Mercedes',
    model: 'S-Class',
    color: 'Black'
}
console.log(car);

// Qs3
let person = {
    Name: 'Ramesh',
    Age: 4,
    City: 'Navi Mumbai'
}
console.log(person);